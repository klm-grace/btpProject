meta {
  name: GET /api/auth/me sans session → 401
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/auth/me
  body: none
  auth: none
}

headers {
  Cookie: sid=invalid-token
}

assert {
  res.status: eq 401
  res.body.success: eq false
  res.body.error.code: eq unauthorized
}
