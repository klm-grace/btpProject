meta {
  name: Health detail forbidden
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/health/detail
  body: none
  auth: none
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 404
  res.body.success: eq false
  res.body.error.code: eq not_found
}
