meta {
  name: Logout exempté du CSRF (POST sans token) → 200
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/auth/logout
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.loggedOut: eq true
}
