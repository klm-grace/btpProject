meta {
  name: GET /api/auth/me - refusé (sans session, cookie jar vide)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/auth/me
  body: none
  auth: none
}

assert {
  res.status: eq 401
  res.body.success: eq false
  res.body.error.code: eq unauthorized
}
