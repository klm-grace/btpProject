meta {
  name: GET /api/auth/me - accès autorisé (requireAuth)
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/auth/me
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.email: eq admin@btp-dev.local
  res.body.data.roles: isDefined
}
