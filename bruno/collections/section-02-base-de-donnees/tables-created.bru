meta {
  name: Tables créées — requête de vérification
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/health
  body: none
  auth: none
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.data.data.status: eq ok
}

docs {
  Vérifie que le serveur répond et que la base de données est accessible.
  Les tables créées sont vérifiées par les tests bun:test.
}
